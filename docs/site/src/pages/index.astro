---
// Coming Soon page — swap back to landing.astro content when ready to launch
import '../styles/global.css';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kiln</title>
  <meta name="robots" content="noindex, nofollow">
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <div class="wrap">
    <div class="logo">
      <img src="/assets/kiln-logo-dark-notext.svg" alt="Kiln" width="112" height="112">
      <span>K<span class="i">I</span>LN</span>
    </div>
    <p class="coming">Coming soon</p>

    <div class="tip-section">
      <p class="tip-label">Support the project</p>
      <div class="wallets">
        <div class="wallet">
          <span class="chain">Solana</span>
          <span class="domain">kiln3d.sol</span>
          <code class="addr" title="Click to copy" onclick="navigator.clipboard.writeText('2jJUNvsDWGUrFqSVXokjS6253MPcMXGAhcvZsM5G55TS')">2jJUNv...G55TS</code>
        </div>
        <div class="wallet">
          <span class="chain">Ethereum</span>
          <span class="domain">kiln3d.eth</span>
          <code class="addr" title="Click to copy" onclick="navigator.clipboard.writeText('0xe46D8557C3d93632e2D519Ebe9e42daff869217a')">0xe46D...9217a</code>
        </div>
      </div>
    </div>
  </div>
</body>
</html>

<style>
  body {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
  }
  .wrap {
    text-align: center;
    padding: 24px;
  }
  .logo {
    display: inline-flex;
    align-items: center;
    gap: 24px;
    margin-bottom: 64px;
  }
  .logo span {
    font-size: 56px;
    font-weight: 700;
    letter-spacing: 0.2em;
    color: var(--text);
  }
  .logo .i { color: var(--accent); }
  .coming {
    font-size: 24px;
    font-weight: 500;
    color: var(--text-muted);
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  /* Tip / donation section */
  .tip-section {
    margin-top: 80px;
    padding-top: 32px;
    border-top: 1px solid var(--border);
  }
  .tip-label {
    font-size: 13px;
    font-weight: 500;
    color: var(--text-muted);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 20px;
  }
  .wallets {
    display: flex;
    gap: 24px;
    justify-content: center;
    flex-wrap: wrap;
  }
  .wallet {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: 16px 24px;
    border: 1px solid var(--border);
    border-radius: 10px;
    background: var(--bg-card);
    min-width: 200px;
  }
  .wallet .chain {
    font-size: 11px;
    font-weight: 600;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .wallet .domain {
    font-size: 18px;
    font-weight: 600;
    color: var(--accent);
  }
  .wallet .addr {
    font-family: var(--mono, monospace);
    font-size: 12px;
    color: var(--text-muted);
    cursor: pointer;
    transition: color 0.15s;
  }
  .wallet .addr:hover { color: var(--text); }

  @media (max-width: 480px) {
    .wallets { flex-direction: column; align-items: center; }
    .wallet { width: 100%; max-width: 280px; }
  }
</style>
